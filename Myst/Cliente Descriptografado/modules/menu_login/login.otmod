Module
  name: menu_login
  description: Manages enter game and character list windows
  author: edubart
  website: www.otclient.info

  load-later:
    - game_things
    - game_features

  @onLoad: |
    dofile 'login'
    dofile 'characterlist'
    -- Precisa vir depois de login.lua: estende a tabela EnterGame dele.
    dofile 'pko_register'
    EnterGame.init()
    init()
    EnterGame.refreshOnline()

  @onUnload: |
    EnterGame.terminate()
    terminate()
